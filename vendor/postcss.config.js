// This file is configured to be minimal to prevent Vite from processing
// CSS with Tailwind as a Node.js module. The project instead uses the
// Tailwind CSS CDN script tag included in `index.html`.
export default {
  plugins: [],
};
